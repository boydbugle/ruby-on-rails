<h1>Photos#index</h1>


  <p>
    <% @photos.each do |photo| %>
        <%= photo.tag%>
        <%= link_to "Edit", edit_photo_path(photo) %>
        <%= link_to "Delete", photo_path(photo), data: {confirm: "delete photo?", method: "delete"} %>
    <% end %>
 </p>
<br />
<%= link_to "Post Photo", new_photo_path %>
