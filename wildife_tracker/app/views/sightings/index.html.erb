<div class="image">
    <h3>Amboseli National Park</h3>

    <br />
     <button class="btn btn-success"><%= link_to "New Sighting", new_sighting_path %></button>
    </div>

    <br />
    <div id="map">  

    </div><div id="map">  

    </div>

    <table>
          <tr>
            <th>Region</th>
            <th>Warden</th>
            <th>Zone</th>
          </tr>
    <% @sightings.each do |sighting| %>
      <tr >
        <td><%= link_to sighting.region, sighting_path(sighting) %></td>
        <td><%= link_to sighting.warden %></td>
        <td><%= link_to sighting.zone %></td>
      </tr>
    <% end %>
    </table>

    